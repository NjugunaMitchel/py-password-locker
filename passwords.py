import unittest
class User:

# users list
  user_list  = []

  def save_user(self):
      user.user_list.append(self)

  '''
  initialize a class
  '''
  def __init__(self,Fname,Sname,username,Password,email):
      '''
      users class 'blueprint'
      '''
      self.Fname = Fname
      self.Sname = Sname
      self.username = username
      self.Password = Password
      self.email = email 

      def test_init(self):
          self.assertEqual(self.Users.Fname = Fname)
          self.assertEqual(self.Users.Sname = Sname)
          self.assertEqual(self.Users.password = password)
          self.assertEqual(self.Users.username = username)
          self.assertEqual(self.Users.email = email)

     if __name__ == '__main__':
         unittest.main()

    def save_user(self):
        self.Users.save_user()
        self.assert(len(password.user_list),1)

